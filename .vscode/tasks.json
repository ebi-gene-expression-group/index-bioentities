{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "create-bioentities-schema",
            "type": "shell",
            "command": "bash",
            "args": [
                "-lc",
                "set -a; source ../.env ; set +a; ./create-bioentities-schema.sh"
            ],
            "options": {
                "cwd": "${workspaceFolder}/bin"
            },
            "presentation": {
                "clear": true
            }
            
        }
    ]
}